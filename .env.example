# Media Editor Environment Variables

# Application Configuration
NODE_ENV=production
PORT=3000

# Docker Compose Configuration
# Custom port for accessing the application (default: 8080)
APP_PORT=8080

# VPS Deployment (GitHub Actions)
# Add these to your GitHub repository secrets
# DOCKER_USERNAME=your-dockerhub-username
# DOCKER_PASSWORD=your-dockerhub-password
# VPS_HOST=your-vps-ip-address
# VPS_USERNAME=root
# VPS_PASSWORD=your-vps-password
# VPS_SSH_KEY=your-private-ssh-key
# VPS_SSH_PORT=22
# VPS_DEPLOY_PATH=/opt/media-editor  (custom deployment path)
# Repository will be automatically cloned using GitHub Actions context

# Optional: Domain Configuration
# DOMAIN=media-editor.com
# EMAIL=admin@media-editor.com
